# ── Brain Server (brain-server.py) ─────────────────────────────────────────────
Flask==3.1.3
Werkzeug==3.1.8
python-dotenv>=1.0.0        # required — auto-loads .env at server startup

# Google Gemini
google-genai==1.66.0

# OpenAI
openai==2.37.0

# Text-to-Speech
gTTS==2.5.4
mutagen==1.47.0

# Robotics / IK
# NOTE: pinocchio must be installed via conda-forge BEFORE pip packages:
#   conda install -c conda-forge pinocchio
pin-pink==4.0.0          # PINK IK solver (wraps pinocchio)
robot_descriptions==1.22.0
quadprog==0.1.13
qpsolvers==4.8.2

# Numerics
numpy==2.2.6

# Networking
requests==2.32.5

# ── MuJoCo Visualiser (test_in_mujoco.py, generate_mapping.py) ─────────────────
mujoco==3.8.1
pygame==2.6.1

# ── Test Client (test-client.py) ───────────────────────────────────────────────
# Already covered above (requests, pygame)

# ── NAO Client (nao-client.py) — Python 2 only ─────────────────────────────────
# nao-client.py targets the NAOqi SDK which requires Python 2.7.
# It cannot be installed via pip in a standard Python 3 environment.
#
# On the NAO / Pepper robot (or Choregraphe virtual robot):
#   1. Download the NAOqi Python 2 SDK from:
#      https://www.aldebaran.com/en/support/nao-6/downloads-softwares
#   2. Add the SDK to PYTHONPATH:
#      export PYTHONPATH=/path/to/pynaoqi-sdk/lib/python2.7/site-packages:$PYTHONPATH
#   3. Install Python 2 dependencies:
#      pip2 install requests pygame
